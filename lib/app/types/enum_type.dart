
enum ImageMode { whiteMode, blackMode, colorMode, darkMode }

enum DioRequest { post, get, put, patch, delete }

enum PasswordStrength { exposed, weak, medium, strong, secure }

enum ButtonType { normal, small, normalOutline, smallOutline}

enum DialogButtonType {normal, outline, text}